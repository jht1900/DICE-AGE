/**/#include "qMenu.h"#include "MenuMgr.h"MenuHandle fileMenu;MenuHandle appleMenu;SetupMenus()	{	MenuHandle	m;		m = NewMenu(appleID, "\p\024" );	AppendMenu(m, "\pAbout Q lisp ;(-");	AddResMenu(m, 'DRVR');	InsertMenu(m, 0);	appleMenu = m;	m = NewMenu(fileID, "\pFile");	AppendMenu(m, "\pNew /N;Open /O;Close ;(- ;Save /S;Save As...;Revert ;(-;Quit /Q");	InsertMenu(m, 0);	fileMenu = m;		m = NewMenu(editID, "\pEdit");	AppendMenu(m, "\pUndo /Z;(- ;Cut /X;Copy /C;Paste /V;Clear ;(- ;Show Clipboard ;(- ;Eval /E");	InsertMenu(m, 0);		m = NewMenu(envID, "\pEnv");	AppendMenu(m, "\pFuncs");	InsertMenu(m, 0);		DrawMenuBar();	}