/**//* ---------------------------------------------------------------------------- */cScanError(msg, a, b, c, d)	char	*msg;	long	a, b, c, d;	{	xprintf(msg, a, b, c, d);	xprintf("\n");	cGoTop();	}	/* ---------------------------------------------------------------------------- */cMemError(msg, len)	char	*msg;	long	len;	{	xprintf("Out of memory! requesting=%ld in %s\n", len, msg);	cGoTop();	}/* ---------------------------------------------------------------------------- */cFatalError(msg, a, b, c, d)	char	*msg;	long	a, b, c, d;	{	xprintf(msg, a, b, c, d);	xprintf("\n");	cGoTop();	}/* ---------------------------------------------------------------------------- */cSyntaxError()	{	cFatalError("Syntax Error");	}	/* ---------------------------------------------------------------------------- */cParseError(msg)	char	*msg;	{	cFatalError(msg);	}