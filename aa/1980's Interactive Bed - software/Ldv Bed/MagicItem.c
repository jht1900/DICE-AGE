/**/#include <WindowMgr.h>#include <DialogMgr.h>#include <ControlMgr.h>#define HorScroll	6#define VerScroll	7#define ReduceScroll	8#define ReduceText		2#define ContrastScroll	9#define ContrastText	3#define GrabButton		4#define ResetButton		5extern Rect	dpRect;extern int grabbing;/* -------------------- MagicItem -------------------------------------- */MagicItem(dp, it)	DialogPtr	dp;	int			it;	{	Rect			rt;	ControlHandle	ctl;	int				type;#if 0	printf("it=%d\n", it);#endif	GetDItem(dp, it, &type, &ctl, &rt);		switch (it)		{		case ResetButton:			MagHreset();			break;					case GrabButton:			grabbing ^= 1;			SetCtlValue(ctl, grabbing);			break;		}	}	/* --------------------------------------------------------------------- */	