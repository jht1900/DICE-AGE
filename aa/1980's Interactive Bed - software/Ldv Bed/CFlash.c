/**/#include <MacTypes.h>#include <MemoryMgr.h>#include <ToolboxUtil.h>#include <QuickDraw.h>#include "HyperXCmd.h"Str255	glob = "\pI am here.";pascal void main(paramPtr)	XCmdBlockPtr	paramPtr;	{	SetUpA4();			DebugStr("\pHI!");		paramPtr->returnValue = PasToZero(paramPtr, &glob);		ZeroToPas(paramPtr,*(paramPtr->params[0]),&glob);		RestoreA4();	}#if 0/* C routines for HyperCard callbacks */#include <XCmdGlue.inc.c>#endif