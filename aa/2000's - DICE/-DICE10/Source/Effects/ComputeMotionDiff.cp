/* History:© 2001 John Henry Thompson. All rights reserved2001.05.22	jht	Split off.*/#include "CCapturePane.h"#define	kBrightNessGain		(100)#define	kBrightLevel		(50)#define CODE_INNER_LOOP2 \				{ \					for (i = 0; i < 3; i++) \					{ \						diffwPix = sourceLinePtr[i] - previousLinePtr[i];  \						if (diffwPix < 0) \							diffwPix = 0; \						if (diffwPix > kBrightLevel) \							 diffwPix += kBrightNessGain; \						monitorLinePtr[i] = diffwPix; \					} \				} \#define CODE_INNER_LOOP \				{ \					for (i = 0; i < 3; i++) \					{ \						diffwPix = sourceLinePtr[i] - previousLinePtr[i];  \						if (diffwPix < 0) \							diffwPix = 0; \						monitorLinePtr[i] = diffwPix; \					} \				} \// ---------------------------------------------------------------------------------void	CCapturePane::ComputeMotionDiff(){	int	i;	int diffwPix;#include "EffectsLoop1.inc"}